# Changesets

This folder is managed by [changesets](https://github.com/changesets/changesets). Run
`pnpm changeset` to add a changeset describing your change; CI turns merged changesets into
version bumps and a published release.
